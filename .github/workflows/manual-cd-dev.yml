name: CD pipiline - Development (frontend)

on:
  workflow_dispatch:

permissions:
  contents: read
  packages: write

jobs:
  build-push-and-deploy:
    name: Build, Push to GHCR & Deploy
    runs-on: ubuntu-latest
    
    environment: development 
    
    steps:
      - name: Checkout code (from branch main)
        uses: actions/checkout@v4
        with:
          ref: 'main'

      - name: Set up Node.js
        uses: actions/setup-node@v4
        with:
          node-version: "20"
          cache: 'npm'

      - name: Install dependencies
        run: npm ci

      - name: Build Next.js app
        run: npm run build

      - name: Log in to GitHub Container Registry (GHCR)
        uses: docker/login-action@v3
        with:
          registry: ghcr.io
          username: ${{ github.actor }}
          password: ${{ secrets.GITHUB_TOKEN }}

      - name: Build and push Docker image to GHCR
        uses: docker/build-push-action@v5
        with:
          context: .
          file: ./Dockerfile
          push: true
          tags: ghcr.io/${{ github.repository_owner }}/frontend:latest
          
      # --- NEXT DEPLOYMENT STEP ---
      # When you're ready to deploy to the server,
      # uncomment and configure this step (e.g., SSH or Webhook).

      # - name: Deploy to Development Server
      #   uses: appleboy/ssh-action@master
      #   with:
      #     host: ${{ secrets.DEV_SERVER_HOST }}
      #     username: ${{ secrets.DEV_SERVER_USER }}
      #     key: ${{ secrets.DEV_SERVER_SSH_KEY }}
      #     script: |
      #       echo "Logging into the server..."
      #       docker pull ghcr.io/${{ github.repository_owner }}/frontend:latest
      #       docker stop frontend-dev || true
      #       docker rm frontend-dev || true
      #       docker run -d --name frontend-dev -p 3000:3000 --restart always \
      #         ghcr.io/${{ github.repository_owner }}/frontend:latest
      #       echo "Frontend deployment completed!"